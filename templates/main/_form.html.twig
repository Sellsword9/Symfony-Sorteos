{# templates/main/_form.html.twig #}

{{ form_start(form) }}
    {{ form_start(form) }}
    {{ form_row(form.money) }}
    {{ form_end(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}